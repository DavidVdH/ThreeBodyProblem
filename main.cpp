#include "evaluationTools.h"

using namespace std;

int main() {

    // ready to run and extend existing parameter space without overlap!
    burrauTesting(10., 0.00001, 0.1, 10, 0.00001, 0.1, 10);

    //usualProblem();
    
    return 0;
}
